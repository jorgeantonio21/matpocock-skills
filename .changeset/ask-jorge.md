---
"mattpocock-skills": minor
---

Add **`ask-jorge`**, this fork's router — `ask-matt`'s map extended with the skills the fork adds on top of upstream (`implement-by-commit`, `implement-by-plan`, `open-pr`). `ask-matt` and its docs page are restored byte-identical to upstream `mattpocock/skills` and stay frozen, so upstream merges never conflict on them; route changes land in `ask-jorge`.

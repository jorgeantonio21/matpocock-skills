---
"mattpocock-skills": patch
---

Route the fork's in-progress skills in **`ask-jorge`**. The main flow gains a fourth question, one agent or two, that places `pair-by-commit` and `pair-by-plan` beside the `implement-by-commit` and `implement-by-plan` loops they mirror. `full-review` sits beside `code-review` as the four-axis review the pair skills gate on. A new **Baselines underneath** section maps `pragmatic-programming` and `idiomatic-rust` as the rule references the `implementer`, `bug-hunter`, and `craft-reviewer` agents carry. The precondition names the agents those skills spawn and how to install them. The docs page is re-synced to match.

---
"mattpocock-skills": patch
---

Load `pragmatic-programming` and the applicable `idiomatic-rust` or `idiomatic-typescript` baseline in `implement`, `implement-by-commit`, and `implement-by-plan`. Run language checks before the commit or human gate. Pass the same requirements to every code-writing worker in `implement-spec`, including final fixes and mergers that edit code. Update the promoted workflows' docs and `ask-jorge` map, and document the baselines as separately installed in-progress prerequisites.

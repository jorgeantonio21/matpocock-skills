# s5-review rubric

The crate under review is `s2-refactor`'s start crate, so the answer key is `../s2-refactor/answer-key.md`. The agent's findings list is the last assistant message in `results/s5-review/<arm>/transcript.jsonl`; `run.sh` also writes it to `final-message.md`.

For each of the thirteen planted patterns, mark whether each arm found it. Then note:

| Question | bare | skill |
| --- | --- | --- |
| Planted patterns found (of 13) | | |
| Findings that are not in the key and are right | | |
| Findings that are not in the key and are wrong or trivial | | |
| Findings that cite a rule by name | | |
| Findings ordered with the type-level ones (5, 12, 13) above the mechanical ones (1, 3, 4) | | |
| Each finding carries a concrete rewrite, not "consider" | | |

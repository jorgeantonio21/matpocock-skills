Fix persisted-data compatibility in this small CLI. Historical version-1 bytes [1, 0] mean Unlimited; [1, n] for nonzero n mean exactly Bounded(n). New version-2 records allow only nonzero capacities. Reject [2, 0], unknown versions, truncation, and trailing bytes. Preserve valid historical semantics without rounding, dropping, or reinterpreting input. Keep the public load, save_new, Capacity, and label API. Add tests using historical bytes and test the CLI's visible output. Preserve the working stdout product and keep simple code where extra types or dependencies provide no benefit. The CLI's two string parameters, explicit error type, and single-threaded execution are acceptable as they stand. The lint policy permits clippy::print_stdout for this binary in both library/binary and all-target passes.

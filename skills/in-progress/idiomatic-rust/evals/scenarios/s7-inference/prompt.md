Fix this inference request scheduler. A policy update changes admission of new work only: already accepted work must execute unchanged and in FIFO order, even when its token count exceeds the new limit or the limit becomes zero. Failed admission must return the entire request unchanged. Removal must report exactly the removed request's children and the number of queued requests remaining at that removal, without reconstructing the removed data with a later lookup. Unknown removal leaves the queue unchanged. Preserve the public API, including mutable Request inputs; the scheduler owns accepted requests. Add regression tests. This component is called synchronously by one owner; there is no requested async runtime, shared-state layer, or dispatch migration. No measured latency budget is provided, so do not claim a performance improvement without evidence.

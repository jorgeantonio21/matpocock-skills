Fix this decoder library's semantic guarantees. `Limit` accepts exactly 0 through 10 via its const constructor, string parsing, and JSON decoding. Invalid input must return an error or None, never panic. Keep `Limit::new` const-compatible and retain `get`, `FromStr`, and `decode`. You may change the parse error type. `negate(NonZeroI32)` must return the exact representable negation or None on overflow while preserving nonzero on success. Keep its signature and support every NonZeroI32 input, including the minimum. Add relevant regression tests, run checks, and explain the change briefly. This is a borrowed-input decoder library; choose only abstractions or dependencies that help this task.
